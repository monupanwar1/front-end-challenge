import { LoginForm } from '@/components/auth/login-form';

export default function LoginPage() {
  return (
    <div className='min-h-screen items-center justify-center py-40'>
      <LoginForm />;
    </div>
  );
}
